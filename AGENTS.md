# Android 客户端开发约定

- 本目录是可单独克隆和提交的 Kotlin / Jetpack Compose Android 仓库；不能依赖父目录、Ubuntu Python 源码或鸿蒙工程才能构建。
- 使用 Windows PowerShell 和 Android Studio/SDK/JDK。由 `build-apk.ps1` 运行 JVM 测试、Lint 并生成 `dist/` 下的 APK；需要设备测试 APK 时添加 `-BuildDeviceTests`。
- 客户端通过 HTTP 调用录像机接口，默认 `http://192.168.10.209:8080`；地址可在客户端设置修改。协议副本位于 `docs/CLIENT_API.md`，同步来源见 `docs/API_SYNC.md`。
- 界面保持方案 B 的五路视频墙、通道详情/回放/事件/通道设置、分类全局设置；支持手机、平板、横竖屏、浅色/深色/跟随系统，默认浅色。
- 网络、媒体、业务状态和 UI 分层；切页/后台释放预览连接，断网恢复后重连。无信号通道独立显示状态，不能阻塞其他通道。
- 保留应用 ID 与既有数据兼容；未获授权不要改变签名或发布到应用商店。`local.properties`、缓存、APK、发布密钥不得提交；Gradle Wrapper JAR 是构建必需文件，必须提交。
- 通道设置保存前读取最新完整配置并合并修改；外观等客户端选项只保存在本机，不写板端配置。
- 验证使用现有 JVM 测试和与改动相关的仪器测试。操作前选定测试设备，不对用户无关设备安装/卸载，不停止全局 adb 或其他模拟器。
- Android 在 Windows 构建，不将 APK、Gradle 缓存、SDK 上传到 ARM 开发板。客户端改动不要求重启板端服务。
- 历史验证文档可保留旧路径；当前文档与脚本使用本仓库根目录，迁移测试另行记录。

## 2026-09-08 原生视觉与全天时间轴

- 最低 Android 12 / API 31。使用 Compose / Material 3 延续鸿蒙客户端的视觉层次；浮动导航使用原生 Surface 与 NavigationBar，不宣称调用鸿蒙 HDS 材质。
- 四个主页面保留悬浮底部导航，根布局缩小内容视口以避开导航（不把保存按钮藏到 dock 后）；通道详情/设置、独立播放器用返回导航。列表的 LocalRecorderContentBottomInset 当前由根布局提供 0，避免重复留白。
- 全局回放仅保留片段列表；通道详情使用 /api/timeline 的完整日索引与垂直时间轴内嵌回放，不能用旧接口最近 200 条伪装全天。详情回放不得添加下载入口。
- 全日查询使用客户端本地日期的真实时区边界，保留午夜相交片段与录像空档；不同类型事件可重叠显示，不杜撰没有记录的持续时长。
## 2026-09-14 手机界面（当前生效）

- 按用户确认的完整手机设计稿实现浅灰/白色分组列表、蓝色操作控件、平面四项底栏；支持浅色、深色及跟随系统。旧悬浮底栏和平板布局为历史设计。
- 本轮只适配手机，最低 Android 12；保留手机横屏全屏播放和通道详情并排布局。
- 通道配置按图像、录像、侦测等子页进入；子页共用草稿，复制和修改均需明确保存。返回可保留草稿，切换录像机不得混用草稿。
- 全局回放为片段列表，下载在独立播放器；通道详情仅用垂直全天时间轴回放，不增加下载。
- AiRec-Host-Android 本轮只制作设计稿，不修改其代码或安装/重启主机。

## 2026-09-14 运行截图验收（当前生效）

- 用户已批准同时实现 AiRec-Host-Android 主机设计；此前“主机只制作设计稿”仅作历史记录。
- UI 修改后需要安装实际 APK，逐页核对用户确认的设计，覆盖深浅色、二级设置、弹层和异常状态，不能用设计 HTML 充当运行截图。
- 手机截图使用测试素材或空通道，README 不放事件页和现场人脸。记录截图所对应的 APK 版本及测试来源。
- 系统日期选择器、文件选择器和动态数据不能伪装为固定设计数据；未通过的验证须明确保留。
